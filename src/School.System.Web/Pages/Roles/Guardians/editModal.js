var abp = abp || {};
abp.modals.GuardianEditModal = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("System");


    };

    return {
        initModal: initModal
    };
};

